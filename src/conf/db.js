/**
 * @description 存储配置
 */
let REDIS_CONF = {
    port: 6379,
    host: '127.0.0.1'
}

let MYSQL_CONF = {
    host: 'localhost',
    user: 'root',
    password: 'Omg?110!ah,',
    datebase: 'koa2-weibo'
}

module.exports = {
    REDIS_CONF,
    MYSQL_CONF
}